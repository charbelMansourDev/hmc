services:
  - type: web
    name: hajj-medical-center
    runtime: static
    buildCommand: echo "Static site - no build step"
    staticPublishPath: .
    headers:
      - path: /*
        name: X-Content-Type-Options
        value: nosniff
